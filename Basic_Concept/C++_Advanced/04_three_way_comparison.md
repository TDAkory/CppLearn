# three-way comparison operator

```cpp
lhs <=> rhs 

// (x <=> y) < 0 is true if x < y
// (x <=> y) > 0 is true if x > y
// (x <=> y) == 0 is true if x and y are equal/equivalent.

auto ans1 = a <=> b;
```
